# Workspace manifest for WASM skills.
# Skill crates are added here as workspace members by new-skill.sh.
# Build with: ./build.sh [skill-name]

[workspace]
members = []
resolver = "2"

[workspace.package]
version = "0.1.0"
edition = "2021"

[workspace.dependencies]
serde_json = "1"
